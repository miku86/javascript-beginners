// it should have a place to store todos
var todos = ["Bier", "Cola"];

// READ: it should log the elements
console.log(todos);

// DELETE: it should delete an element
// CREATE: it should add an element
// UPDATE: it should upadte an element
// it should toggle an element
